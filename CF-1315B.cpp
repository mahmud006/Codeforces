#include<bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin >> t;
    while(t--)
    {
        int a,b,p,sz;
        cin >> a>> b>>p;
        string str;
        cin >> str;
        sz = str.size();
        reverse(str.begin(), str.end());
        for(int i=0; i<str.size(); i++)
        {
            if(str[i])
        }

    }
}
